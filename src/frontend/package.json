{
  "name": "org.icpclive.url-controller",
  "private": true,
  "scripts": {
    "buildOverlay": "npm run build -w overlay",
    "buildAdmin": "npm run build -w admin"
  },
  "workspaces": [
    "overlay",
    "admin"
  ]
}
